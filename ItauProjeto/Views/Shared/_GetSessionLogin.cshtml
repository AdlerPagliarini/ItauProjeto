@model ItauProjeto.Models.ViewModelCompartilhado

<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="@Url.Action("Index", "Home")"><img src="~/Content/images/logo-itau.png" /></a>
        </div>
        <div id="navbar" class="navbar-collapse collapse" aria-expanded="false" style="height: 1px;">
            <ul class="nav navbar-nav">
                <li><a href="@Url.Action("Index", "Home")">Home</a></li>
                <li><a href="@Url.Action("Index", "Cadastrar")">Cadastrar cliente</a></li>
                @{
                    if (!string.IsNullOrEmpty(Model.login))
                    {
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" stlye="color:#fff;">Gerenciar perfil<span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li><a href="@Url.Action("Editar", "Portal", new { id = Model.id })">Editar perfil</a></li>
                                <li><a href="@Url.Action("AlterarSenha", "Portal", new { id = Model.id })">Alterar senha</a></li>
                                <li role="separator" class="divider"></li>
                                @*<li class="dropdown-header">Nav header</li>*@
                                @*<li><a onclick="excluirConta(@Model.id,'@(Url.Action("JsonAjaxExcluir", "Portal"))');" class="pointer">Excluir perfil</a></li>*@
                                <li><a onclick="excluindoCliente();" class="pointer">Excluir perfil</a></li>
                            </ul>
                        </li>
                        <li><a style="color:#fff;">Cliente: @Model.login</a></li>
                        <li class="active"><a href="@Url.Action("Fechar", "Compartilhado")">Sair</a></li>
                    }
                }
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>